import React from 'react';
import Icon from '@ant-design/icons';
import { ReactComponent as SvgIcon } from 'assets/messaging.svg';

const MessaginIcon = () => {
  return <Icon component={SvgIcon} />;
};

export default MessaginIcon;
