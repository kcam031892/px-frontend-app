import React from 'react';
import Icon from '@ant-design/icons';
import { ReactComponent as SvgIcon } from 'assets/projects.svg';

const ProjectsIcon = () => {
  return <Icon component={SvgIcon} />;
};

export default ProjectsIcon;
