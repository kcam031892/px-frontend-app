import { Card } from 'antd';
import styled from 'styled-components';

export const BiographyWrapper = styled.div`
  padding: 0rem 5rem;
`;

export const CardEditor = styled(Card)`
  border-radius: 1rem;
`;
