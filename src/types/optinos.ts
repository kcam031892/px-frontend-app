export interface Country {
  code: string;
  name: string;
}

export interface State {
  id: number;
  name: string;
  countryCode: string;
}

export interface ArtistType {
  code: string;
  name: string;
}
